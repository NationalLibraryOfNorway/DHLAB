name: Deploy new version
on:
  push:
    branches:
      - hotfix/versions


jobs:
  bump_version:
    uses: NationalLibraryOfNorway/DHLAB/.github/workflows/bump-version.yml@hotfix/versions
  upload_package:
    uses: NationalLibraryOfNorway/DHLAB/.github/workflows/upload-package.yml@hotfix/versions
